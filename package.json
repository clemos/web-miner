{
  "name": "web-miner",
  "version": "0.0.1",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js & http-server --cors pub",
    "configure": "cd third-party/cpuminer-multi && ./autogen.sh && emconfigure ./configure",
    "build": "./build.sh"
  },
  "author": "Clément Charmet <cl3mos@gmail.com> (http://github.com/clemos)",
  "license": "ISC",
  "dependencies": {
    "ws": "^3.2.0"
  },
  "devDependencies": {
    "http-server": "^0.10.0"
  }
}
